#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T;
    cin >> T;
    for (int nT=0; nT < T; nT++) {
        int result;

        cout << "Case #" << nT + 1 << " : " << result << endl;
    }
    return 0;
}